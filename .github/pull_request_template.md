## Summary

<!-- What changed and why, in language the requester can follow. -->

## Data-security surface

<!-- Which of these did this change touch: auth, roles, permissions, masking, audit,
     logging, config, external calls, dependencies? Answer explicitly, including "None". -->

## Not done

<!-- Anything you were asked to do but deliberately did not, and why. Write "Nothing" if
     the request was implemented in full. -->

## Checks

- [x] `npm run check` (guardrails), lint, typecheck, build, ruff, mypy, pytest all pass locally
